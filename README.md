## Setup
1. Clonar el repo
2. Crear `.env` a partir de `.env.example`
3. Completar las variables
4. Ejecutar `npm install`
5. Ejecutar `npm run dev`

> Para el sistema de autenticación se requieren credenciales propias de Auth0.